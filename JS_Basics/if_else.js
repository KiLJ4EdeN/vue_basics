let age = 15;

if ((age >= 14) && (age < 19)) {        // logical condition
    stat = "Eligible.";               // executed if condition is true
    } else {                                // else block is optional
    stat = "Not eligible.";           // executed if condition is false
    }


console.log(stat);